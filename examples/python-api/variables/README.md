## Variable Extraction Example

This example demonstrates how to extract all variables and global variables
from a Clingo AST statement using Python's `functools.singledispatch` for
flexible AST traversal.

### Usage

Run the script:

```bash
python example.py
```

It will parse a sample logic program statement and print all variables and
global variables found in the AST.
